#!/usr/bin/env zsh
# sync: Fetch everything and rebase current branch.

CYAN='\033[1;36m'
GREEN='\033[1;32m'
RESET='\033[0m'

echo -e "${CYAN}🔄 Syncing with remote...${RESET}"
git fetch --all --prune
git pull --rebase
echo -e "${GREEN}✅ Local branch is up to date.${RESET}"

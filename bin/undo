#!/usr/bin/env zsh
# undo: Soft reset the last commit. Keep changes staged.

YELLOW='\033[1;33m'
RESET='\033[0m'

echo -e "${YELLOW}⏪ Undoing last commit (keeping changes staged)...${RESET}"
git reset --soft HEAD~1
echo -e "${YELLOW}✅ Last commit undone.${RESET}"

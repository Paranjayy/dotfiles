#!/bin/bash
# Toggle Scroll Lock LED (for RGB keyboard backlight)
LED="input4::scrolllock"
CURRENT=$(brightnessctl --device "$LED" get)
if [ "$CURRENT" -eq 0 ]; then
    brightnessctl --device "$LED" set 1
else
    brightnessctl --device "$LED" set 0
fi

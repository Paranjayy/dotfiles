# Ideas

## Terminal / Shell
- Keep Starship prompt compact with branch, counts, and runtime always visible.
- Use `` for git branch instead of the leaf icon.
- Add per-project tmux sessions when launching Ghostty.
- Keep tmux status bar Theo-inspired, but less crowded.

## Memory
- Append notable prompt/chat preferences into the memory vault after each session.
- Track a simple prompt count and topic tags.
- Keep a semantic versioned changelog for config and memory changes.

## Nice-to-haves
- Show tmux session name in the prompt.
- Add a small OS/battery/time indicator when useful.
- Make Ghostty open directly into the active project session.
- Add prompt history search across past sessions.
- Auto-tag chats by topic and tool used.
- Keep a compact per-project tmux session registry.
- Add one-command project launcher for Ghostty + tmux + editor.
- Store a tiny session summary after each work block.
- Add a quick toggle for "focus mode" terminal output.
- Show git dirty count separately from untracked count.
- Add a minimal status line for current project and task.
- Make the shell prompt show tmux session name only when inside tmux.
- Add a lightweight clock/battery segment that only appears in Ghostty.
- Keep branch icon as a terminal-only glyph so IDE rendering can stay imperfect.
